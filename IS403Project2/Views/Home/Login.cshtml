
@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

@using (Html.BeginForm("Login", "Home", FormMethod.Post))
{
    <label for="inputUsername">Username</label>
    @Html.TextBox("Username", null, new { @class = "form-control", autofocus = "", placeholder = "Enter Username" })
    <br />
    <label for="inputPassword">Password</label>
    @Html.Password("Password", null, new { @class = "form-control", placeholder = "Password" })
    <br />
    <button class="btn btn-primary btn-lg" type="submit">Login</button>

    <h3 style="color:red">@ViewBag.errorMessage</h3>
}

<p>@Html.ActionLink("Create Account", "CreateUser", "Home", null, new { @class = "btn btn-default btn-lg" })</p>